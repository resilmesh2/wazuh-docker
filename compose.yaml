services:
  resilmesh.tap.wazuh.manager:
    image: wazuh/wazuh-manager:4.9.0
    hostname: resilmesh.tap.wazuh.manager
    container_name: resilmesh.tap.wazuh.manager
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 655360
        hard: 655360
    ports:
      - 1514:1514
      - 1515:1515
      - 514:514/udp
      - 55000:55000
    environment:
      - INDEXER_URL=https://resilmesh.tap.wazuh.indexer:9200
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - FILEBEAT_SSL_VERIFICATION_MODE=full
      - SSL_CERTIFICATE_AUTHORITIES=/etc/ssl/root-ca.pem
      - SSL_CERTIFICATE=/etc/ssl/filebeat.pem
      - SSL_KEY=/etc/ssl/filebeat.key
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - filebeat_etc:/etc/filebeat
      - filebeat_var:/var/lib/filebeat
      - agentless:/var/ossec/agentless
      - api_configuration:/var/ossec/api/configuration
      - logs:/var/ossec/logs
      - queue:/var/ossec/queue
      - var_multigroups:/var/ossec/var/multigroups
      - wodles:/var/ossec/wodles
      - ./config/wazuh_active_response:/var/ossec/active-response/bin
      - ./config/wazuh_etc:/var/ossec/etc
      - ./config/wazuh_indexer_ssl_certs/root-ca-manager.pem:/etc/ssl/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.manager.pem:/etc/ssl/filebeat.pem
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.manager-key.pem:/etc/ssl/filebeat.key
      - ./config/wazuh_integrations:/var/ossec/integrations
      - ./ossec.conf:/wazuh-config-mount/etc/ossec.conf
    networks:
      resilmesh_network:
        ipv4_address: 172.19.0.100

  resilmesh.tap.wazuh.indexer:
    image: wazuh/wazuh-indexer:4.9.0
    hostname: resilmesh.tap.wazuh.indexer
    container_name: resilmesh.tap.wazuh.indexer
    restart: always
    ports:
      - ${INDEXER_PORT}:9200
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - indexer-data:/var/lib/wazuh-indexer
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-indexer/certs/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.indexer-key.pem:/usr/share/wazuh-indexer/certs/resilmesh.tap.wazuh.indexer.key
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.indexer.pem:/usr/share/wazuh-indexer/certs/resilmesh.tap.wazuh.indexer.pem
      - ./config/wazuh_indexer_ssl_certs/admin.pem:/usr/share/wazuh-indexer/certs/admin.pem
      - ./config/wazuh_indexer_ssl_certs/admin-key.pem:/usr/share/wazuh-indexer/certs/admin-key.pem
      - ./config/wazuh_indexer/wazuh.indexer.yml:/usr/share/wazuh-indexer/opensearch.yml
      - ./config/wazuh_indexer/internal_users.yml:/usr/share/wazuh-indexer/opensearch-security/internal_users.yml
    networks:
      - resilmesh_network

  resilmesh.tap.wazuh.dashboard:
    image: wazuh/wazuh-dashboard:4.9.0
    hostname: resilmesh.tap.wazuh.dashboard
    container_name: resilmesh.tap.wazuh.dashboard
    restart: always
    ports:
      - ${DASHBOARD_PORT}:5601
    environment:
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - WAZUH_API_URL=https://resilmesh.tap.wazuh.manager
      - DASHBOARD_USERNAME=kibanaserver
      - DASHBOARD_PASSWORD=kibanaserver
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - dashboard-config:/usr/share/wazuh-dashboard/data/wazuh/config
      - dashboard-custom:/usr/share/wazuh-dashboard/plugins/wazuh/public/assets/custom
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.dashboard.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem
      - ./config/wazuh_indexer_ssl_certs/resilmesh.tap.wazuh.dashboard-key.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-dashboard/certs/root-ca.pem
      - ./config/wazuh_dashboard/opensearch_dashboards.yml:/usr/share/wazuh-dashboard/config/opensearch_dashboards.yml
      - ./config/wazuh_dashboard/wazuh.yml:/usr/share/wazuh-dashboard/data/wazuh/config/wazuh.yml
    depends_on:
      - resilmesh.tap.wazuh.indexer
    links:
      - resilmesh.tap.wazuh.indexer:resilmesh.tap.wazuh.indexer
      - resilmesh.tap.wazuh.manager:resilmesh.tap.wazuh.manager
    networks:
      - resilmesh_network

volumes:
  api_configuration:
  logs:
  queue:
  var_multigroups:
  integrations:
  active_response:
  agentless:
  wodles:
  filebeat_etc:
  filebeat_var:
  indexer-data:
  dashboard-config:
  dashboard-custom: